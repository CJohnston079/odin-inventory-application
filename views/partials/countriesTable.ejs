<table>
	<thead>
		<tr>
			<th>Country</th>
			<th>Books</th>
		</tr>
	</thead>
	<tbody>
		<% countries.map(country => { %>
		<tr>
			<td><a href="countries/<%= country.name %>"><%= country.name %></a></td>
			<td><%= country.books %></td>
		</tr>
		<% }) %>
	</tbody>
</table>
